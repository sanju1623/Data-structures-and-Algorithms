class Solution {
    public int missingNumber(int[] nums) {
     
        int k=nums.length;
        int sum=(k*(k+1))/2;
        int total=0;
        for(int i=0;i<nums.length;i++){
            total=total+nums[i];
        }
        return  sum-total;
    }
}
