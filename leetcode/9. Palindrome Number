class Solution {
    public boolean isPalindrome(int x) {
       if(x<0)
           return false;
        int p=0,c=x;
        while(x>0){ 
            p=10*p+x%10;
            x=x/10;
        }
        
        if(p==c)
        return true;
        else
        return false;
    }
}
